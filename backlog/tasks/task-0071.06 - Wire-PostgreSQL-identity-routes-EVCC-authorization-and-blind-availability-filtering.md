---
id: TASK-0071.06
title: >-
  Wire PostgreSQL identity routes, EVCC authorization, and blind-availability
  filtering
status: In Progress
assignee:
  - '@Codex'
created_date: '2026-09-01 19:03'
updated_date: '2026-09-08 16:08'
labels:
  - postgresql
  - identity
  - backend
dependencies:
  - TASK-0071.05
references:
  - docs/requirements/functional/fr/FR-060.md
  - docs/requirements/functional/fr/FR-062.md
  - docs/requirements/functional/fr/FR-079.md
  - docs/requirements/functional/fr/FR-084.md
  - docs/requirements/quality/qr/QR-003.md
  - docs/requirements/quality/qr/QR-006.md
documentation:
  - server/docs/postgres-anonymous-event-compatibility.md
  - docs/design/architecture/adr/ADR-009.md
  - docs/design/architecture/adr/ADR-010.md
modified_files:
  - server/routes/postgres_event_routes.go
  - server/routes/events.go
  - server/routes/auth.go
  - server/errs/errors.go
parent_task_id: TASK-0071
priority: high
type: feature
ordinal: 139300
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Third delivery subtask of TASK-0071, depends on TASK-0071.05. Wire PostgreSQL routes to the identity foundation: event creation issues and binds an owner Event Visitor Identity, event fetch exposes public identifiers with blind-availability filtering, response CRUD authorizes via source EVCC or an associated Platform Identity session with an explicit selected-response contract for multiple responses per Event Visitor Identity, and sign-in associates browser Event Visitor Identities with the session's Platform Identity. Owner-power enforcement (settings edits, archive, delete) is deliberately out of scope and tracked in a follow-up task.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 PostgreSQL event creation issues the owner Event Visitor Identity and EVCC, returns the public eventVisitorId, and binds the owner Event Visitor Identity on the event for blind-availability visibility; no Event Owner Edit Token or settings authorization is implemented in this subtask
- [ ] #2 PostgreSQL event fetch exposes the browser eventVisitorId and opaque response public_ids; blind-availability reads expose a non-owner only responses the visitor is authorized to manage with no other-response counts, while the owner views all responses
- [ ] #3 PostgreSQL response create, update, and delete authorize through a source EVCC or an authenticated session whose Platform Identity is associated with the owning Event Visitor Identity, and never treat a raw eventVisitorId or Platform Identity value supplied as a credential as authorization (QR-003)
- [ ] #4 PostgreSQL response APIs support multiple responses per Event Visitor Identity with an explicit selected-response creation, edit, and deletion contract and preserve transactional num_responses updates
- [ ] #5 Sign-in associates the current browser Event Visitor Identities with the session visitor's private Platform Identity (find-or-create) without MongoDB queries or migration (FR-079)
- [ ] #6 MongoDB request shapes, behavior, and credentials are byte-identical to before
- [ ] #7 Route and frontend regression coverage verifies PostgreSQL identity behavior, blind filtering, and MongoDB non-regression (TASK-0071 AC #5 through AC #9, AC #11)
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 All acceptance criteria are satisfied
- [ ] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [ ] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [ ] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Bind owner Event Visitor Identity during PostgreSQL creation. Resolve browser identity from public query ID and private cookie, preserving a known public identity without granting authority when credentials are absent. Filter blind reads by proven visitor or associated session, expose opaque response IDs plus canEdit, and require explicit responseId/createResponse selection for transactional mutations. Associate browser identities on sign-in using cookie proof.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-08 progress: PostgreSQL event creation transaction now binds the owner visitor and issues its cookie. Reads return eventVisitorId, canCreateResponse, opaque response publicId and canEdit; blind reads filter by proven authority and omit non-owner counts. Response mutations require responseId or explicit createResponse and lock the event row for count updates. New POST /auth/visitor-identities associates cookie-proven browser visitors without MongoDB identity lookup; existing OAuth/OTP request payloads remain unchanged. `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test go test ./postgres ./routes -count=1` passed before the latest account-recovery test expansion. The expanded TestPostgresVisitorIdentityContract subsequently passed independently, including same-account recovery on another browser and rejection of forged association by a different account. Legacy PostgreSQL guest credential expectations in anonymous_event_compatibility_test.go were replaced with the new PG identity contract while retaining MongoDB checks. Remaining: update shared events.go Swagger annotations for new response fields and review all read/mutation paths.
<!-- SECTION:NOTES:END -->

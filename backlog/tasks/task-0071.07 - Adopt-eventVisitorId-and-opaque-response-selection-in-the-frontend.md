---
id: TASK-0071.07
title: Adopt eventVisitorId and opaque response selection in the frontend
status: Done
assignee:
  - '@Codex'
created_date: '2026-09-01 19:03'
updated_date: '2026-09-08 18:04'
labels:
  - frontend
  - identity
dependencies:
  - TASK-0071.06
references:
  - docs/requirements/functional/fr/FR-073.md
  - docs/requirements/functional/fr/FR-079.md
  - docs/requirements/functional/fr/FR-001.md
  - docs/requirements/functional/fr/FR-060.md
documentation:
  - frontend/AGENTS.md
  - docs/design/architecture/adr/ADR-009.md
  - docs/terminology/glossary.md
modified_files:
  - frontend/src/types/transport.ts
  - frontend/src/composables/event/eventTransportBoundary.ts
  - frontend/src/composables/event/responseSubmissionBoundary.ts
  - frontend/src/composables/schedule_overlap/scheduleOverlapStorage.ts
  - frontend/src/utils/services/UserService.ts
  - frontend/src/views/Auth.vue
parent_task_id: TASK-0071
priority: high
type: feature
ordinal: 140300
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Fourth delivery subtask of TASK-0071, depends on TASK-0071.06. Adopt the PostgreSQL identity boundary in the frontend: decode and encode eventVisitorId and opaque response public_ids in the transport layer, persist a per-event non-authorizing eventVisitorId in browser-local storage that survives sign-out, key response selection by opaque public_id, and send current Event Visitor Identities for association at sign-in. Keep the MongoDB guest-credential flow untouched.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 transport.ts decodes and encodes eventVisitorId and opaque response public_ids at the transport boundary only, consumed through the event and response boundary composables
- [x] #2 A per-event eventVisitorId is persisted in browser-local storage, retained across sign-out and browser sessions until browser-local data is cleared, is non-authorizing, and is never a raw Platform Identity value (FR-073)
- [x] #3 PostgreSQL response selection and editing are keyed by the opaque response public_id instead of authUser._id or display names
- [x] #4 Sign-in sends the current browser Event Visitor Identities for Platform Identity association
- [x] #5 MongoDB guest-credential flow (guestOwnershipCollection, guestId/guestEditToken) is unchanged
- [x] #6 Frontend lint, typecheck, build, and unit tests pass
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Add explicit visitor and response identity fields to transport/internal types, a separate per-event browser identity/selection storage boundary, and sign-in association calls. Reuse response list/editor UI with server-provided canEdit and opaque publicId, leaving legacy guest credential storage untouched. Cover boundary persistence and explicit selection in unit and browser tests.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-08 progress: Added separate visitorIdentityStorage.ts for per-event public identity and opaque response selection; no EVCC values or legacy guest credentials are stored there. Transport/create boundaries retain public IDs. OAuth/OTP service calls associate retained browser identities after sign-in. Existing response editor/list/preferences now consume publicId/canEdit and explicit selection, including signed-in visitors. New visitorIdentityStorage.test.ts covers identity/selection separation, legacy-storage non-mutation, same-name response selection and server permissions. Initial full unit run: 140 files/1038 tests passed, new 2 tests failed due missing localStorage test fixture; fixture fixed and targeted test rerun passed 2/2. Frontend lint passed after fixes; build and typecheck passed earlier, before final generated API and follow-up edits. Remaining: full final checks and real Firefox editing verification; inspect any remaining authUser._id selection paths, plugin behavior, and deletion paths.

Final session hardening 2026-09-08: fixed the respondents-list delete path in RespondentsList.vue to submit { responseId } through withEventVisitorIdentity for PostgreSQL events (previously the legacy Mongo payload would be rejected with select-response-or-explicitly-create) and to clear the stale selected response when the deleted one was selected; added a PostgreSQL reanimation guard earlier in useAvailabilityData.ts. Unit regressions added for both the PostgreSQL explicit-selection delete branch and the unchanged Mongo legacy payload branch. Final checks all pass: lint, fmt:check, typecheck, build, full unit suite (141 files / 1042 tests), firefox-desktop e2e for identities (2 tests incl. new deletion check) and the plugin contract (1 test).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Adopted the PostgreSQL identity boundary in the frontend: transport.ts/eventTransportBoundary.ts decode eventVisitorId, opaque response publicId, and canEdit at the transport boundary; responseSubmissionBoundary.ts encodes the explicit createResponse/responseId contract; visitorIdentityStorage.ts persists the per-event non-authorizing eventVisitorId and the selected response in localStorage, surviving reload and sign-out; UserService sends retained browser identities for association at OAuth/OTP sign-in. Event.vue maps the unchanged plugin set-slots contract onto explicit selection (named response, else selected, else create), the availability editor and respondents list key selection by publicId, and the respondents-list delete submits responseId for PostgreSQL events while reanimateAvailability no longer infers ownership from authUser._id. MongoDB guest credentials and flows untouched. Covered by visitorIdentityStorage.test.ts, RespondentsList delete-contract unit tests, and firefox e2e for retention, multi-response editing, plugin set-slots, and deletion. Frontend lint, fmt:check, typecheck, build, and the full unit suite (141 files, 1042 tests) pass.
<!-- SECTION:FINAL_SUMMARY:END -->

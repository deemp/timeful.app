---
id: TASK-0071.04
title: Add PostgreSQL platform and event visitor identity schema and repository
status: Done
assignee:
  - '@Codex'
created_date: '2026-09-01 19:02'
updated_date: '2026-09-08 18:03'
labels:
  - postgresql
  - identity
  - backend
dependencies: []
references:
  - docs/requirements/functional/fr/FR-073.md
  - docs/requirements/functional/fr/FR-079.md
  - docs/requirements/functional/fr/FR-001.md
  - docs/requirements/functional/fr/FR-060.md
  - docs/requirements/quality/qr/QR-003.md
documentation:
  - server/docs/postgres-anonymous-event-compatibility.md
  - docs/design/architecture/adr/ADR-009.md
  - docs/design/architecture/adr/ADR-010.md
  - docs/terminology/glossary.md
modified_files:
  - server/migrations/
  - server/postgres/types.go
  - server/postgres/repository.go
  - server/postgres/pool.go
parent_task_id: TASK-0071
priority: high
type: feature
ordinal: 137300
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
First delivery subtask of TASK-0071. Add the PostgreSQL-only identity schema and repository layer that the rest of the foundation builds on: private Platform Identities (PVI), event-scoped Event Visitor Identities (EVI), hashed EVCC storage, and Event Visitor Identity-owned responses with composite integrity and opaque public response IDs. Backfill existing PostgreSQL response rows. MongoDB remains untouched.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A PostgreSQL migration creates platform_identities, event_visitor_identities, and event_visitor_credentials relations with UUIDv7 internal identifiers, an opaque non-authorizing public eventVisitorId on event_visitor_identities, and credential values stored only as hashes
- [x] #2 postgres_event_responses gains a NOT NULL event_visitor_identity_id with a composite (event_id, event_visitor_identity_id) foreign key such that database integrity rejects an event and owner Event Visitor Identity mismatch
- [x] #3 postgres_event_responses gains an opaque response public_id used as the public response identifier
- [x] #4 Existing PostgreSQL guest, account, and legacy name-keyed response rows are backfilled to Event Visitor Identity ownership without data loss; account rows find-or-create private Platform Identities from their external Mongo user id strings
- [x] #5 server/postgres repository methods support identity, credential, and EVI-owned response persistence and lookups, including lookups by opaque response public_id
- [x] #6 MongoDB persistence, request behavior, and guest credentials remain unchanged
- [x] #7 Repository and route test coverage verifies the migration, backfill integrity, composite constraint rejection, and MongoDB non-regression
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [ ] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Add UUIDv7 platform/event visitor and hashed credential relations, backfill PostgreSQL response owners and opaque response IDs, then implement repository identity operations and composite ownership constraints. Validate migration and repository behavior against isolated PostgreSQL and retain MongoDB route regression coverage. Continue dependent credential, route, frontend, and verification subtasks in order.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-08 progress: Added server/migrations/20260908160000_visitor_identities.sql, private platform identity and event visitor repositories, hashed credential persistence, opaque response IDs, and composite event/visitor ownership. Migration applied to isolated test PostgreSQL. TestVisitorIdentityRepository covers stable account identity, association, revocation, multiple responses and cross-event constraint rejection. TestVisitorIdentityMigrationBackfill uses temporary tables to exercise the existing migrations plus the new migration with account, guest-ID, and legacy-name response rows. Latest isolated `go test ./postgres -count=1` passed after adding the backfill test. Remains In Progress pending foundation review and final verification; migration intentionally refuses automatic downgrade because the legacy schema cannot represent multiple responses per visitor.

Final verification 2026-09-08: isolated `docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test go test ./postgres ./routes -count=1` passed (postgres ok, routes ok) on the committed foundation (2ada63a4). Migration TestVisitorIdentityMigrationBackfill exercised the migration chain over account, guest-ID, and legacy-name rows; TestVisitorIdentityRepository covers stable account identity, association, revocation, multi-response, and composite event/owner constraint rejection. Migration remains applied on the isolated test PostgreSQL; downgrade stays intentionally refused.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added server/migrations/20260908160000_visitor_identities.sql with platform_identities, event_visitor_identities (opaque public eventVisitorId), and hashed event_visitor_credentials; postgres_event_responses gained event_visitor_identity_id with composite (event_id, event_visitor_identity_id) integrity and opaque public_id, plus a backfill for existing guest/account/name-keyed rows with find-or-create Platform Identities from external Mongo user id strings. server/postgres gained identity, credential, and EVI-owned response repository operations including lookups by public_id. Verified by isolated go test ./postgres ./routes (backfill, association, revocation, constraint rejection) with MongoDB untouched.
<!-- SECTION:FINAL_SUMMARY:END -->

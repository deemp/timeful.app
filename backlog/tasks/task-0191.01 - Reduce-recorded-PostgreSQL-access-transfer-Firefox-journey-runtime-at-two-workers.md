---
id: TASK-0191.01
title: >-
  Reduce recorded PostgreSQL access-transfer Firefox journey runtime at two
  workers
status: Done
assignee:
  - OpenCode
created_date: '2026-09-10 06:35'
updated_date: '2026-09-10 07:31'
labels: []
dependencies:
  - TASK-0192
references:
  - e2e/specs/timed-event-access-transfer-firefox.spec.ts
  - e2e/helpers/actor-context.ts
  - e2e/playwright.config.ts
modified_files:
  - e2e/config/bundled-frontend.ts
  - e2e/config/tooling.ts
  - e2e/specs/timed-event-access-transfer-firefox.spec.ts
  - e2e/AGENTS.md
  - docs/environments.md
parent_task_id: TASK-0191
priority: medium
type: enhancement
ordinal: 207000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Optimize the PostgreSQL access-transfer browser journeys while retaining their existing scenario assertions, isolated actor credentials, real reloads, and source/target/stranger video recordings.
The first four tests with Firefox desktop, two workers, PostgreSQL enabled, and E2E_VIDEO=on produced two passes and guest/owner approval timeouts during target reload on 2026-09-10.
Preserve the existing 30-second timeout and parallel execution; measure runtime improvements rather than masking failures with retries, skipped coverage, or serialization.
Baseline artifacts: /tmp/opencode/timeful-e2e-artifacts/2026-09-10T06-29-21.112592041Z-p1493027/.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 All eight PostgreSQL access-transfer tests pass with Firefox desktop at two workers and E2E_VIDEO=on without increasing timeouts or weakening existing scenario assertions.
- [x] #2 The first four tests pass in repeated two-worker recorded runs, with timings and setup/teardown evidence recorded.
- [x] #3 Actor credentials remain isolated and retained videos cover every browser actor through its final scenario action, including actors closed early.
- [x] #4 Relevant E2E package checks pass and runtime changes and remaining performance limitations are documented in task notes.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Use the recorded first-four failure baseline and run guest approval alone before changes.
2. Replace full-load navigation waits with DOM readiness plus existing semantic UI assertions; close the stranger after its final denial assertion and move end-of-test actor cleanup into the existing fixture.
3. Measure the first four journeys with two workers and all videos; refine only measured bottlenecks while preserving scope and assertions.
4. Verify repeated first-four runs, full eight-test coverage, retained actor videos, E2E package checks, and graph update; record objective evidence and finalize only if criteria pass.

Navigation/cleanup alone still failed guest/owner; smaller viewport experiment also failed and was removed. Measure opt-in E2E_FRONTEND=bundled: fresh test-mode build and Playwright-owned Vite preview with identical isolated proxy, assets under the invocation artifact directory, ordinary dev mode unchanged. This reduces repeated unbundled module loading while retaining all recorded browser journeys.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Baseline guest alone with videos passed in 23.5s (38.2s invocation); after navigation/cleanup changes it passed in 22.8s (37.8s invocation). First-four two-worker run after those changes: 2 passed / guest and owner timeouts, 1.6m, setup 10.410s and teardown 1.430s; artifacts 2026-09-10T06-41-58.91485791Z-p1542360. Smaller 1280x720 viewport experiment gave 1 pass / 3 timeouts in 1.6m and was removed; artifacts 2026-09-10T06-44-52.17460498Z-p1558268. Host load average measured after that run: 28.47 / 16.41 / 9.96; small timing differences are not controlled evidence of improvement.

User approved a separately tracked consent watcher race fix as TASK-0192 after bundled/parallel page startup exposed a missing dialog independently of the 30-second budget. Guest isolated failed at 20.4s; trace /tmp/opencode/timeful-e2e-artifacts/task-0191-01-seed-guest/ shows grant-association returned confirmationRequired:true while the dialog remained absent. Performance verification depends on that fix.

## Session stop / resume — 2026-09-10
User requested implementation notes and continuation in a later session immediately after the first successful four-test two-worker run.
Keep this task In Progress; no acceptance criteria or Definition of Done items have been checked.

### Current implementation
- Access-transfer goto/reload calls now use domcontentloaded followed by existing semantic UI assertions; real reloads and scenario assertions remain.
- Removed sequential try/finally actor closes from the spec; the existing actor-context fixture closes contexts concurrently and finalizes recordings during teardown.
- In the three approval journeys, target and stranger pages are created concurrently with Promise.all.
- The stranger context closes immediately after its final denial assertion, preserving its recording through that action while avoiding idle recording through the remaining target journey.
- Split account setup into async seedAccount using promisified execFile and verifySignIn using normal OTP verification; guest account seeding overlaps target/stranger page startup, but target authentication still happens at its original scenario point.
- Original 1440x1600 Firefox viewport remains; the smaller viewport experiment was removed because it did not improve measured outcomes.
- No test timeout, retries, worker cap, serial grouping, API authority checks, consent assertions, or video retention policy was weakened.
- e2e/config/tooling.ts selects opt-in E2E_FRONTEND=bundled only in test tooling mode; ordinary dev-server mode remains the default.
- New e2e/config/bundled-frontend.ts runs a fresh Vite build in mode:test, outputs frontend-dist under the invocation artifact directory, and serves it with Playwright-owned Vite preview using the existing isolated test host/port/proxy plus strictPort.
- These assets are separate from frontend/dist and dedicated production-style fixtures; this mode does not replace production-asset coverage.
- Build timing is printed to stderr as [e2e frontend] build so Playwright surfaces it; normal build stdout is suppressed by Playwright webServer defaults.
- The bundled entry imports createFrontendDevServerConfig from frontend/config/tooling.ts; package typecheck/lint for the new import remains pending.

### Investigation
Bundled assets reduced module-loading cost, but bundled mode alone still gave two approval timeouts.
The guest trace in task-0191-01-bundled-four-1 showed target/stranger newPage calls taking ~3.35s/~4.43s sequentially, motivating concurrent creation despite initial contention concerns.
Bundled + concurrent pages first-four run: 3 passed / guest failed; owner 25.7s, signed-in 17.3s, reload guest 9.4s, guest 32.2s; 1.2m total, build 4.386s, stack setup 9.136s, teardown 1.566s.
Guest then failed alone in 20.4s after async seed overlap, exposing a consent watcher race independently of the test budget.
TASK-0192 tracks the user-approved fix; this task depends on it.

### Latest passing verification with TASK-0192 fix
Command from e2e/: E2E_FRONTEND=bundled E2E_ARTIFACTS_RUN_ID=task-0191-01-consent-fixed-four-1 E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true E2E_VIDEO=on npm run test:e2e -- --project=firefox-desktop --workers=2 specs/timed-event-access-transfer-firefox.spec.ts --grep 'Source approves the exact target code|Approved guest transfer survives target reload'
Result: all 4 passed, 1.1m Playwright invocation duration, build 2.642s, stack setup 10.538s, teardown 1.686s.
Per-test reported durations: guest approval 28.3s, owner approval 28.2s, signed-in approval 18.5s, approved guest reload 13.0s.
This is ONE successful run, not repeated reliability evidence; guest/owner still have limited headroom.
Isolated guest after the consent fix: passed in 19.3s (42.0s invocation; build 3.206s; setup 10.903s; teardown 1.640s).
Successful-run traces were not enabled; E2E_VIDEO=on retains recordings, default traces retain only failures.

### Artifacts
All directories are under /tmp/opencode/timeful-e2e-artifacts/.
Relevant labels: task-0191-01-bundled-guest, task-0191-01-bundled-four-1, task-0191-01-parallel-pages-guest, task-0191-01-parallel-pages-four, task-0191-01-seed-guest, task-0191-01-consent-fixed-guest, task-0191-01-consent-fixed-four-1.
Failed runs attached source video.webm, video-2-target-1, and video-3-stranger-1 including early-closed stranger recordings.
Final successful-run video inventory and readability/end-of-journey review remain pending; do not claim that inspection was completed.

### Resume next session
1. Inspect staged and unstaged diffs; E2E changes and this task were already staged when the user stopped, while frontend race files and TASK-0192 were unstaged/untracked.
2. Repeat the first-four bundled/video command with a NEW unique E2E_ARTIFACTS_RUN_ID, then run all eight with the same flags and no grep.
3. Verify retained actor videos and inspect any failure before further changes; consider --trace=on for diagnosis, noting its additional cost.
4. Verify ordinary dev mode still works, at least the scoped guest journey; successful two-worker evidence currently applies ONLY to opt-in bundled mode.
5. Document E2E_FRONTEND=bundled, fresh test-mode build, isolated proxy, artifact-directory assets, benchmark tradeoff, and limited headroom.
6. Run E2E lint/fmt:check/typecheck and all required frontend checks because TASK-0192 changes runtime code; only focused formatting and the consent unit test have run so far.
7. Run Markdown formatting and graphify update . after final code changes; neither has run for this implementation yet.
8. Finalize TASK-0192 and this task only against verified criteria; parent TASK-0191 has broader remaining work.
No repeated first-four or full-eight run started after the last success; the subsequent empty tool call was aborted before the user requested session stop.
Last actual invocation completed normal isolated stack teardown.
No commit was created; preserve unrelated backlog/backlog.md, .codex/, and handoff archives.

### Verification completed — 2026-09-10
- All eight PostgreSQL access-transfer tests pass at the default two workers with recording and bundled test-mode assets: `E2E_FRONTEND=bundled E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true E2E_VIDEO=on npm run test:e2e -- --project=firefox-desktop --workers=2 specs/timed-event-access-transfer-firefox.spec.ts` → 8 passed in 1.5m (wall 90s; stack setup 9.371s; teardown 1.477s).
- First-four repeated: separate run → 4 passed in 1.1m (wall 69s; setup 9.037s; teardown 1.258s). Slowest tests: guest 28.1s, owner 27.7s, signed-in 20.0s, approved guest reload 14.4s. Artifacts under `/tmp/opencode/timeful-e2e-artifacts/task-0191-01-verify-four-1/` and `task-0191-01-verify-eight-1/`.
- Retained actor videos: the first-four run contains source (`video.webm`), target (`video-2-target-1.webm`), and stranger (`video-3-stranger-1.webm`) recordings for the guest/owner/signed-in approval tests; the two reload tests retain source and target only because they open no stranger page. `ffprobe` confirms every WebM is readable with a finite duration (stranger ends at ~10s after its early close; target and source run to the end).
- E2E package checks from `e2e/`: `npm run lint`, `npm run fmt:check`, `npm run typecheck` all pass. Fixed a pre-existing `eslint(no-non-null-assertion)` error in the spec by replacing `guestAccountEmail!` with an explicit guard; re-verified the guest journey still passes.
- Documentation: `e2e/AGENTS.md` and `docs/environments.md` now document opt-in `E2E_FRONTEND=bundled`, the unchanged dev-server default, the repeated two-worker passing results, and the one-worker sequential fallback.
- Remaining limitation: the default unbundled dev server still exceeds the 30-second budget for the parallel approval journeys; bundled mode is opt-in and does not replace the production-asset projects.

### Finalization — 2026-09-10
All acceptance criteria and Definition of Done items verified with the evidence above and marked complete. Status set to Done.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Speeds up the recorded PostgreSQL access-transfer Firefox journeys to fit the two-worker default without weakening assertions, timeouts, isolation, or recording. Adds an opt-in `E2E_FRONTEND=bundled` mode that builds fresh test-mode assets and serves them from a Playwright-owned preview using the isolated host/port/proxy, while the default dev server is unchanged. The spec now navigates with `domcontentloaded` plus existing semantic assertions, starts target/stranger pages concurrently, overlaps guest account seeding with page startup, closes the stranger right after its final denial assertion, and finalizes actor teardown in the actor-context fixture. Verified: all eight tests pass at two workers with `E2E_VIDEO=on` and bundled assets (1.5m; setup 9.4s, teardown 1.5s); a repeated first-four run passed (1.1m); retained WebM recordings cover every actor, including the early-closed stranger; e2e lint/fmt:check/typecheck pass. Remaining limitation: the default unbundled dev server still exceeds the 30-second budget for the parallel approval journeys, so bundled mode is opt-in and the documented one-worker sequential fallback remains available.
<!-- SECTION:FINAL_SUMMARY:END -->

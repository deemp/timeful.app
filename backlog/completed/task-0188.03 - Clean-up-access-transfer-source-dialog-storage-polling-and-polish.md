---
id: TASK-0188.03
title: 'Clean up access-transfer source dialog storage, polling, and polish'
status: Done
assignee:
  - OpenCode
created_date: '2026-09-09 12:50'
updated_date: '2026-09-09 17:52'
labels: []
dependencies:
  - TASK-0188
references:
  - frontend/src/components/event/EventAccessTransfer.vue
  - frontend/src/composables/transfer/transferBoundary.ts
  - frontend/src/views/AccessTransfer.vue
  - frontend/src/components/event/EventAccessTransfer.test.ts
modified_files:
  - frontend/src/components/event/EventAccessTransfer.vue
  - frontend/src/components/event/EventAccessTransfer.test.ts
  - frontend/src/composables/transfer/transferBoundary.ts
  - frontend/src/composables/transfer/transferBoundary.test.ts
  - frontend/src/views/AccessTransfer.vue
  - frontend/src/views/AccessTransfer.test.ts
  - e2e/specs/timed-event-access-transfer-firefox.spec.ts
parent_task_id: TASK-0188
priority: medium
type: task
ordinal: 195000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Hygiene and polish follow-ups from the TASK-0071.02 review on the source transfer dialog. rememberTransfer appends forever so localStorage grows with every transfer ever created, and the two-second dialog poll issues one status request per saved identifier, multiplying stale traffic as history accumulates. Errors are conflated: every failure in EventAccessTransfer, including clipboard copy, reports the transfer-unavailable message. Transfer states render as raw enum strings, the target page failure alert uses info styling, and granted-access numbering shifts when entries are filtered out.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Saved transfer identifiers are pruned once they are no longer revocable and stored lists do not grow with transferred history
- [x] #2 Status polling issues requests proportional to active grants rather than all saved transfers and stops when the dialog closes
- [x] #3 Clipboard copy failure shows an action-specific message instead of the transfer-unavailable message
- [x] #4 Transfer states render in user-facing language and the target page failure alert uses error styling
- [x] #5 Granted-access numbering stays stable when history entries are removed
- [x] #6 Frontend unit tests cover storage pruning polling reduction and the action-specific error copy
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Add regression tests using real storage helpers and mocked transport for legacy-history pruning, deduplicated polling, terminal/current transfer lifecycle, transient failures, close/unmount cleanup, stable grant numbering, and action-specific errors.
2. Normalize legacy saved string arrays at the storage boundary into numbered active-transfer records with a constant-size next-number counter; deduplicate identifiers and remove only terminal/non-revocable or definitively unavailable handles, retaining pending/approved transfers and transient failures.
3. Poll each tracked transfer once per cycle while the dialog is open, reconcile current state and grant history together, stop tracking terminal handles, and preserve stable grant numbers across removals/reloads.
4. Name failed source actions, render friendly transfer status labels, and use error styling on target failure alerts.
5. Run scoped regressions, all required frontend checks, the isolated Firefox access-transfer browser suite, Markdown formatting, and graphify update; record evidence and finalize the task.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Dependency TASK-0188 is the umbrella parent coordinating this subtask, rather than a prerequisite implementation.
Existing unrelated worktree changes were preserved.

Regression-first evidence: the original implementation failed tests for duplicate/history polling, transient failures hiding grants, and clipboard error copy; the wrong-code persistence regression remained passing.

AC 1/2/5/6: real-storage component regressions verify legacy deduplication, terminal/non-revocable and 403 pruning, active-only polling, one request for the current transfer, pending/approved retention, completed-current polling cessation, transient 500 retention, stable numbering after revoke and remount, and polling cleanup on close/unmount.
Boundary tests cover legacy migration, deduplication, monotonic numbers after complete pruning, malformed storage, and blocked writes.

AC 3/4: component tests verify clipboard-specific feedback survives polling and target error alerts use type=error.
Firefox exercised friendly approval errors/statuses and verified storage empties after guest revocation and completed session transfer.

Required frontend checks passed: npm run lint; npm run fmt:check; npm run typecheck; npm run build; npm run test:unit (145 files / 1066 tests).
Following final lint-driven narrowing changes, typecheck/build/format and all 18 focused transfer tests passed again.
E2E lint, fmt:check, and typecheck passed.

Isolated PostgreSQL-enabled Firefox suite passed 7/7: E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true npm run test:e2e -- --project=firefox-desktop specs/timed-event-access-transfer-firefox.spec.ts.
Initial invocation hit the tool's 120-second process limit after four passes; rerun with a 300-second tool limit completed successfully in 2.5 minutes.

npm run format:markdown and git diff HEAD --check passed.
Managed Backlog Markdown is excluded by the formatter and remains MCP-authored.
graphify update . completed (5058 nodes / 8324 edges); it reported zero-node JSON inputs (tasks.json, timezones.json, swagger.json), refreshed community hub labels, and generated the aggregated HTML view.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## Summary
- Replaced append-only saved identifiers with deduplicated numbered active-transfer records and a small next-number counter, retaining legacy storage compatibility.
- Prune terminal/non-revocable and definitively unavailable handles while preserving pending/approved transfers and retryable failures.
- Poll tracked transfers once each per cycle, stop polling on close/unmount, and remove completed current transfers from subsequent polls.
- Preserve grant numbers across removals and reloads; show action-specific source errors, friendly transfer statuses, and error-styled target alerts.

## Verification
- All required frontend checks passed, including 1066 unit tests across 145 files.
- All 18 focused transfer tests passed after final lint fixes.
- All 7 isolated Firefox access-transfer browser tests passed with PostgreSQL creation enabled.
- E2E lint, formatting, and typecheck; Markdown formatting; and diff whitespace checks passed.
- Updated the local code knowledge graph.
<!-- SECTION:FINAL_SUMMARY:END -->
